# openarm


Arm factory and analytical IK (via ssik) for the OpenArm robot.


Provides `create_openarm_left_arm()` and the OpenArm-left kinematic
constants. Loads its model from the `openarm_assets` package.
